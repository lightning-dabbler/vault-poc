services:
  vault-diagrams:
    container_name: vault-diagrams
    image: vault-poc-diagrams
    pull_policy: build
    volumes:
      - .:/vault-poc
    networks:
      - vault-diagrams-network
    stdin_open: true
    tty: true

networks:
  vault-diagrams-network:
